import os

# Download the tar.gz file directly to Colab
!wget -O ghcnd_all.tar.gz https://www1.ncdc.noaa.gov/pub/data/ghcn/daily/ghcnd_all.tar.gz

# Extract the contents
!tar -xvzf ghcnd_all.tar.gz

# Optional: Load extracted data into Pandas
import pandas as pd

data = pd.read_csv('ghcnd-stations.txt', delim_whitespace=True, header=None)
print(data.head())
